# Next goal

Do only these three things, in order:

1. Run a real ten-profile monitor-only soak and restart with zero actions, drops, recovery HTTP, or unresolved journals.
2. Observe one naturally occurring fresh leader fill in the existing bounded two-account configuration and measure source receive -> decision -> socket send -> exchange response -> reconciled truth. Do not manufacture a leader trade or widen caps merely to obtain a sample.
3. Build an independent accounting/copyability oracle for candidate ranking using held-out data, fees, funding, spread, slippage, latency, rounding, and missing-history flags.

Do not add Linux, a node, VPN routing, another supervisor, more gates, or broad test infrastructure unless one of these steps demonstrates a concrete need.
